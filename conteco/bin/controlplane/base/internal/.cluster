#!/usr/bin/env bash
(
    . executionplane-invoke "$0 $@"
    case "$1" in
		deploy)
			executionplane docker stack $@
			;;
		remove)
			executionplane docker stack $@
			;;
        *)
            executionplane-error "Method not recognised: $1."
            ;;
    esac
    executionplane-wait-for-output-parser
    executionplane-complete
)