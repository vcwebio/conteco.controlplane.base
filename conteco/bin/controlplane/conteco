#!/usr/bin/env bash

# the conteco handler maps onto the .invoke API entry point
# the arguments are supplied as is to the .invoke method
(
    .invoke echo $@ > >( /conteco/bin/controlplane/base/internal/output-parser ) 2> >( /conteco/bin/controlplane/base/internal/output-parser );
)
