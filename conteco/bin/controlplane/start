#!/usr/bin/env bash
pwd_volume="$1"
if [[ "$pwd_volume" == *"\\"* ]] ; then
	pwd_volume=$( echo $pwd_volume | tr '\\' '\/' | tr -d ':' )
	pwd_volume="/${pwd_volume//\/\//\/}"
	pwd_volume="${pwd_volume,,}"
	export CONTECO_OS="windows"
else
	export CONTECO_OS="linux"
fi
if [[ "$2" != "" ]] ; then
	export CONTECO_REALM="$2"
fi
export PS1="${CONTECO_ECOSYSTEM}#$ "
export CONTECO_PWD_VOLUME="$pwd_volume"
bash
