echo "Starting controlplane.${CONTECO_NAME}"
${CONTECO_SUDO}docker run -it -v /var/run/docker.sock:/var/run/docker.sock -v /conteco/pwd:/conteco/pwd ${CONTECO_REALM}/${CONTECO_ECOSYSTEM}.controlplane.${CONTECO_NAME} --interactive start linux
