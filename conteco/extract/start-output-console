${CONTECO_SUDO}docker run -it -v /var/run/docker.sock:/var/run/docker.sock -v $(pwd):/infometis/pwd ${CONTECO_REALM}/${CONTECO_ECOSYSTEM}.controlplane.${CONTECO_NAME} --interactive ${CONTECO_OS} output-console
